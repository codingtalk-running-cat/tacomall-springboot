<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="store.tacomall.mapper.goods.GoodsMapper">

    <resultMap id="goods" type="store.tacomall.entity.goods.Goods">
        <id property="id" column="id"></id>
        <collection property="goodsItem" column="id" select="store.tacomall.mapper.goods.GoodsItemMapper.getGoodsItemByGoodsId"></collection>
    </resultMap>


    <select id="getGoodsPage" resultMap="goods">
        SELECT *
        FROM goods
    </select>

    <select id="getGoods" resultMap="goods">
        SELECT *
        FROM goods
        WHERE id=#{id}
    </select>


</mapper>