server:
  port: 4001

spring:
  profiles:
    active: @environment@

logging:
  level:
    com.baomidou: debug

mybatis-plus:
  type-aliases-package: store.tacomall.common.mapper
  mapper-locations: classpath:/mapper/*Mapper.xml